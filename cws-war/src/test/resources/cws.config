# General Settings, which is being controlled by the Build Process.

# The current version of the CWS, this is set in the Maven pom.xml, and here it
# is simply set from the build process and is used for the "version" request.
cws.version=${project.version}
